# -----------------------------
# Servidor da aplica��o
# -----------------------------
server.port=8080

# -----------------------------
# Oracle (PDB XEPDB1 via SERVICE_NAME)
# Porta do host = 1522 (mapeada pelo Docker)
# -----------------------------
spring.datasource.url=jdbc:oracle:thin:@//localhost:1522/XEPDB1
spring.datasource.username=APP_LUIGARAH
spring.datasource.password=G9!pV3#z12LUIGARAHAPPoracle
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Opcional (ajuda o Hibernate a qualificar as tabelas)
spring.jpa.properties.hibernate.default_schema=APP_LUIGARAH

# -----------------------------
# JPA / Hibernate
# -----------------------------
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# (Opcional: fixe o dialeto; o Spring costuma detectar)
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect

# -----------------------------
# Pool de conex�es (Hikari) ? recomendado
# -----------------------------
spring.datasource.hikari.pool-name=Hikari-Oracle
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.connection-test-query=SELECT 1 FROM dual

# -----------------------------
# Swagger
# -----------------------------
springdoc.swagger-ui.path=/

# -----------------------------
# CORS
# -----------------------------
app.cors.origens-permitidas=http://localhost:3000,https://luigarah.vercel.app

# -----------------------------
# Logs
# -----------------------------
logging.level.com.luigara=DEBUG